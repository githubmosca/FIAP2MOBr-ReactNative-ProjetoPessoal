import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  eventContainer: {
    marginBottom: 20,
  },
  year: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  description: {
    fontSize: 16,
    marginLeft: 10,
  },
});

export default styles;
