import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    marginBottom: 20,
    alignItems: 'center',
  },
  image: {
    width: 100,
    height: 100,
    borderRadius: 10,
  },
  caption: {
    marginTop: 10,
    textAlign: 'center',
    fontSize: 14,
  },
});

export default styles;
